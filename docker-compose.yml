version: '3.7'

services:
 postgresql:
  image: postgres
  ports:
   - '5432:5432'
  volumes:
   - ./data:/var/lib/postgres
  environment:
   POSTGRESQL_DATABASE: app
   POSTGRESQL_USER: app
   POSTGRESQL_PASSWORD: 12345